# ─────────────────────────────────────────────────────────────
# CaloPlan v2（coloplan-v2）环境变量模板
#
# 部署/开发使用：cp .env.example .env 后按需修改。
# 优先级：环境变量 > .env > src/config/microservices.ts 默认值。
# 未设置的变量会自动回退到 src/config/microservices.ts 中的默认地址。
# 本文件入库；.env 已被 .gitignore 忽略，不会提交。
# ─────────────────────────────────────────────────────────────

# 微服务地址（默认值见 src/config/microservices.ts）
VITE_USER_URL=http://localhost:8000
VITE_META_URL=http://localhost:9093
VITE_CHAT_URL=http://localhost:9095
VITE_FILE_URL=http://localhost:9094

# 演示模式：true=未登录时展示 Demo 数据（原型开箱即用）；false=仅真实数据
VITE_DEMO_MODE=true
